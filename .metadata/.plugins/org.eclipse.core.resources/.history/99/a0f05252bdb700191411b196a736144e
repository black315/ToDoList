<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8"/>
<title>ToDoリスト</title>
</head>
	<body>
    	<h3>TODOリスト</h3>
    	<div>
            <div th:each="todo : ${toDoListForm}">
                <div class="todo">
                	<p th:text="${todo.name}"></p>
                	<p th:text="期限：　${todo.deadline}"></p>
                	<p th:text="作成日：${todo.createdAt}"></p>
                </div>
            </div>
	    </div>
	</body>
</html>